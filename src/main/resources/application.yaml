spring:
  datasource:
    username: restaurant_reservation
    password: restaurant_reservation
    url: jdbc:mysql://localhost:3306/restaurant_reservation
      
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
